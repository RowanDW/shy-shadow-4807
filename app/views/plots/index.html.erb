<h1>Plots Index</h1>

<% @plots.each do |plot| %>
  <div id="plot-<%= plot.id %>">
    <h3>Plot #<%= plot.number %></h3>
    <h4>Plants:</h4>
    <% plot.plants.each do |plant| %>
      <p>- <%= plant.name %></p>
    <% end %>
  </div>
<% end %>
